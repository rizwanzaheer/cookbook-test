const initialState = {
  allProperties: '',
  favoriteProperties: [],
};

const favoritePropertiesReducer = (state = initialState, action) => {
  return state;
};

export default favoritePropertiesReducer;
